blank_issues_enabled: true
contact_links:
  - name: knip itself (scan results, false positives)
    url: https://github.com/webpro-nl/knip/issues
    about: knip-gui only visualizes knip's report — issues with what knip detects belong upstream.
